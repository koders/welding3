/**
 * OrderController
 *
 * @description :: Server-side logic for managing orders
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */

module.exports = {
  //getOrders: function (req, res) {
  //  OrderService.getOrders(function (orders) {
  //    res.json(orders);
  //  });
  //},
  //addOrder: function (req, res) {
  //  var order = (req.body.value) ? req.body.value : undefined;
  //  OrderService.addOrder(order, function (success) {
  //    res.json(success);
  //  });
  //},
  //removeOrder: function (req, res) {
  //  var order = (req.body.value) ? req.body.value : undefined;
  //  OrderService.removeOrder(order, function (success) {
  //    res.json(success);
  //  });
  //}
};

